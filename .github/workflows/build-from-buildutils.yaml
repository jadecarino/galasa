#
# Copyright contributors to the Galasa project
#
# SPDX-License-Identifier: EPL-2.0
#
name: Build from Buildutils

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['modules/buildutils/**']

env:
  REGISTRY: ghcr.io
  NAMESPACE: galasa-dev
  BRANCH: ${{ github.ref_name }}

jobs:

  build-buildutils:
    name: Build the 'buildutils' module of Galasa
    uses: ./.github/workflows/buildutils.yaml
    secrets: inherit

  build-framework:
    name: Build the 'framework' module of Galasa
    needs: [build-buildutils]
    uses: ./.github/workflows/framework.yaml
    secrets: inherit

  build-extensions:
    name: Build the 'extensions' module of Galasa
    needs: [build-framework]
    uses: ./.github/workflows/build-extensions.yaml
    secrets: inherit

  build-managers:
    name: Build the 'managers' module of Galasa
    needs: [build-framework]
    uses: ./.github/workflows/build-managers.yaml
    secrets: inherit

  build-obr:
    name: Build the 'obr' module of Galasa
    needs: [build-extensions, build-managers]
    uses: ./.github/workflows/build-obr.yaml
    secrets: inherit